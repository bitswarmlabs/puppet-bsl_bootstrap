###########################################
# Init settings for bsl_bootstrap
###########################################

LOGFILE=<%= scope.lookupvar('bsl_bootstrap::puppetmaster::config::init_service_log') %>
PUPPET=<%= scope.lookupvar('bsl_bootstrap::puppetmaster::config::puppet_binary') %>
